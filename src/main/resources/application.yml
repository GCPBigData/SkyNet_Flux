spring:
  r2dbc:
    url: r2dbc:postgresql://localhost:5432/skynet?schema=skynet
    username: postgres
    password: debian23

logging:
  level:
    org:
      springframework:
        data:
          r2dbc: DEBUG